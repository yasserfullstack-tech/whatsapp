name: Platform admin migration rebuild

on:
  push:
    branches: [feat/platform-admin]

permissions:
  contents: write

jobs:
  reconcile:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 12
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_DB: whatsapp
          POSTGRES_USER: whatsapp
          POSTGRES_PASSWORD: whatsapp
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U whatsapp -d whatsapp"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgres://whatsapp:whatsapp@localhost:5432/whatsapp
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/platform-admin
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2
      - run: bun install
      - name: Generate reconciled platform-admin migration
        run: bun run db:generate -- --name=platform-admin
      - name: Apply migrations to clean PostgreSQL
        run: bun run db:migrate
      - name: Verify migrated schema
        run: bun run db:verify
      - name: TypeScript
        run: bun run typecheck
      - name: Commit generated migration and remove temporary workflow
        run: |
          rm .github/workflows/platform-admin-rebuild.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add packages/db/drizzle .github/workflows/platform-admin-rebuild.yml
          git commit -m "fix(platform-admin): reconcile migration with main"
          git push origin HEAD:feat/platform-admin
