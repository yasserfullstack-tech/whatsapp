{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "reqps" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 6, "x": 0, "y": 0 },
      "id": 1,
      "targets": [{ "expr": "sum(rate(whatsapp_http_requests_total[1m]))", "legendFormat": "requests/s", "refId": "A" }],
      "title": "API request rate",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 6, "x": 6, "y": 0 },
      "id": 2,
      "targets": [{ "expr": "histogram_quantile(0.95, sum by (le) (rate(whatsapp_http_request_duration_seconds_bucket[5m])))", "legendFormat": "p95", "refId": "A" }],
      "title": "API p95 latency",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 6, "x": 12, "y": 0 },
      "id": 3,
      "targets": [{ "expr": "rate(whatsapp_recipient_sends_total[1m])", "legendFormat": "recipients/s", "refId": "A" }],
      "title": "Recipient send rate",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 6, "x": 18, "y": 0 },
      "id": 4,
      "targets": [{ "expr": "sum by (queue) (rate(whatsapp_failed_jobs_total[5m]))", "legendFormat": "{{queue}}", "refId": "A" }],
      "title": "Failed jobs rate",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 9, "w": 12, "x": 0, "y": 8 },
      "id": 5,
      "targets": [{ "expr": "whatsapp_queue_depth", "legendFormat": "{{queue}} / {{state}}", "refId": "A" }],
      "title": "Queue depth",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 9, "w": 12, "x": 12, "y": 8 },
      "id": 6,
      "targets": [
        { "expr": "whatsapp_worker_active_jobs", "legendFormat": "active {{queue}}", "refId": "A" },
        { "expr": "whatsapp_worker_configured_concurrency", "legendFormat": "configured {{queue}}", "refId": "B" }
      ],
      "title": "Worker concurrency",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 0, "y": 17 },
      "id": 7,
      "targets": [{ "expr": "histogram_quantile(0.95, sum by (le) (rate(whatsapp_webhook_processing_lag_seconds_bucket[5m])))", "legendFormat": "p95", "refId": "A" }],
      "title": "Webhook processing lag p95",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "rows/s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 8, "y": 17 },
      "id": 8,
      "targets": [{ "expr": "rate(whatsapp_csv_rows_processed_total[5m])", "legendFormat": "rows/s", "refId": "A" }],
      "title": "CSV processing rate",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "reqps" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 16, "y": 17 },
      "id": 9,
      "targets": [{ "expr": "sum by (status) (rate(whatsapp_http_errors_total[5m]))", "legendFormat": "HTTP {{status}}", "refId": "A" }],
      "title": "HTTP error rate",
      "type": "timeseries"
    }
  ],
  "refresh": "5s",
  "schemaVersion": 39,
  "tags": ["whatsapp", "observability"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "WhatsApp Platform Overview",
  "uid": "whatsapp-overview",
  "version": 1
}
